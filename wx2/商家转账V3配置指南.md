# 商家转账到零钱V3 API 配置指南

## 📌 重要说明

您在微信支付商户平台看到的是**商家转账到零钱（V3新版API）**，这是微信支付推荐使用的新接口。

---

## 一、您的当前配置（根据截图）

### ✅ 已开通状态
- ✅ API发起：已开启
- ✅ 页面发起：已开启
- ✅ 转账场景：分销返佣（ID: 1001）

### 💰 额度配置
```
出资账户：运营账户
单日转账额度：100,000.00元
单笔转账最低金额：0.10元

分销返佣场景配置：
- 单笔转账最高：500元
- 单日向单用户转账最高：20,000元
```

### 🔐 安全配置
- 转账验密入：已设置管理员
- 免密额度：100.00元

---

## 二、V3 API与V2 API对比

| 特性 | V2 企业付款（旧） | V3 商家转账（新） ✅ |
|------|------------------|-------------------|
| API地址 | mmpaymkttransfers/promotion/transfers | v3/transfer/batches |
| 签名方式 | MD5 | SHA256-RSA |
| 证书类型 | API证书 | API证书 + 平台证书 |
| 单笔限额 | 固定2000元 | 可配置（您的是500元） |
| 场景管理 | 无 | 支持多场景 ✅ |
| 姓名校验 | 可选 | 可选（更安全） |
| 推荐度 | ⚠️ 即将废弃 | ⭐⭐⭐⭐⭐ 强烈推荐 |

**建议：使用V3 API**

---

## 三、获取必需的配置信息

### 3.1 商户号（mchid）

```
位置：微信支付商户平台 → 账户中心 → 商户信息
示例：1234567890
```

### 3.2 商户API证书序列号（serial_no）

```bash
# 方法1：使用命令查看
openssl x509 -in apiclient_cert.pem -noout -serial

# 输出示例：
serial=1A2B3C4D5E6F7G8H9I0J

# serial_no = 1A2B3C4D5E6F7G8H9I0J
```

```
方法2：在商户平台查看
位置：账户中心 → API安全 → 管理API证书
```

### 3.3 APIv3密钥（apiv3_key）

```
位置：账户中心 → API安全 → 设置APIv3密钥

注意：
1. 是32位字符串
2. 不是API密钥（api_key）
3. 设置后无法查看，请妥善保管
4. 用于回调通知的解密
```

### 3.4 商户私钥（private_key）

```bash
# 从apiclient_key.pem文件中读取全部内容
cat apiclient_key.pem

# 内容格式：
-----BEGIN PRIVATE KEY-----
MIIEvQIBADANBgkqhkiG9w0BAQEFA...
...（完整私钥内容）...
-----END PRIVATE KEY-----
```

### 3.5 平台证书（platform_cert）

平台证书用于验证微信支付的回调签名，需要通过API获取。

**获取方法**：

```javascript
// 调用获取平台证书API
GET https://api.mch.weixin.qq.com/v3/certificates

// 返回的证书是加密的，需要用apiv3_key解密
```

---

## 四、配置代码参数

### 4.1 修改 cashback-handler-v3.js

打开文件：`wx2/uniCloud-aliyun/cloudfunctions/articleWx/cashback-handler-v3.js`

```javascript
constructor() {
  this.config = {
    appid: 'wxf7ee79349bd957b8',              // ← 小程序APPID
    mchid: '1545803671',               // ← 商户号
    serial_no: 'YOUR_SERIAL_NO',        // ← 证书序列号
    apiv3_key: 'lishuai4323811lishuai4323811lish',     // ← APIv3密钥（32位）
    
    // 商户私钥（完整内容）
    private_key: `-----BEGIN PRIVATE KEY-----
MIIEvQIBADANBgkqhkiG9w0BAQEFA...
...（您的完整私钥）...
-----END PRIVATE KEY-----`,
  };
  
  // 平台证书（从API获取后填写）
  this.platform_cert = `-----BEGIN CERTIFICATE-----
MIIDXTCCAkWgAwIBAgIUZa...
...（平台证书完整内容）...
-----END CERTIFICATE-----`;
}
```

### 4.2 更新 articleWx/index.obj.js

```javascript
// 修改引用
const { CashbackHandlerV3 } = require('./cashback-handler-v3.js');

// 在 processCashback 方法中
async processCashback(bargain_record_id, user_id) {
  // ... 前面代码不变 ...
  
  // 使用V3处理器
  const cashbackHandler = new CashbackHandlerV3();
  const result = await cashbackHandler.processCashback({
    // ... 参数不变 ...
  });
  
  // ... 后续代码不变 ...
}
```

---

## 五、获取平台证书

平台证书用于验证回调，必须先获取。

### 5.1 创建获取证书脚本

创建文件：`wx2/uniCloud-aliyun/cloudfunctions/getCertificates.js`

```javascript
const crypto = require('crypto');
const https = require('https');

// 配置信息
const config = {
  mchid: 'YOUR_MCH_ID',
  serial_no: 'YOUR_SERIAL_NO',
  apiv3_key: 'YOUR_APIV3_KEY_32',
  private_key: `-----BEGIN PRIVATE KEY-----
...您的私钥...
-----END PRIVATE KEY-----`
};

// 获取平台证书
async function getCertificates() {
  const url = 'https://api.mch.weixin.qq.com/v3/certificates';
  const timestamp = Math.floor(Date.now() / 1000);
  const nonce_str = crypto.randomBytes(16).toString('hex');
  
  // 构建签名串
  const signStr = `GET\n/v3/certificates\n${timestamp}\n${nonce_str}\n\n`;
  
  // 生成签名
  const sign = crypto.createSign('RSA-SHA256');
  sign.update(signStr);
  const signature = sign.sign(config.private_key, 'base64');
  
  // 构建Authorization
  const authorization = `WECHATPAY2-SHA256-RSA2048 mchid="${config.mchid}",nonce_str="${nonce_str}",signature="${signature}",timestamp="${timestamp}",serial_no="${config.serial_no}"`;
  
  // 发送请求
  const options = {
    method: 'GET',
    headers: {
      'Authorization': authorization,
      'Accept': 'application/json',
      'User-Agent': 'uniCloud-CertTool/1.0'
    }
  };
  
  return new Promise((resolve, reject) => {
    const req = https.request(url, options, (res) => {
      let data = '';
      res.on('data', (chunk) => { data += chunk; });
      res.on('end', () => {
        const result = JSON.parse(data);
        if (result.data && result.data.length > 0) {
          // 解密证书
          const encryptedCert = result.data[0].encrypt_certificate;
          const decrypted = decryptCertificate(
            encryptedCert.ciphertext,
            encryptedCert.nonce,
            encryptedCert.associated_data,
            config.apiv3_key
          );
          console.log('平台证书：');
          console.log(decrypted);
          resolve(decrypted);
        } else {
          reject(new Error('获取证书失败'));
        }
      });
    });
    
    req.on('error', reject);
    req.end();
  });
}

// 解密证书（AES-256-GCM）
function decryptCertificate(ciphertext, nonce, associated_data, key) {
  const decipher = crypto.createDecipheriv(
    'aes-256-gcm',
    key,
    nonce
  );
  decipher.setAuthTag(Buffer.from(ciphertext, 'base64').slice(-16));
  decipher.setAAD(Buffer.from(associated_data));
  
  let decrypted = decipher.update(
    Buffer.from(ciphertext, 'base64').slice(0, -16),
    null,
    'utf8'
  );
  decrypted += decipher.final('utf8');
  
  return decrypted;
}

// 执行
getCertificates().catch(console.error);
```

### 5.2 运行脚本获取证书

```bash
# 本地运行
node getCertificates.js

# 输出的证书内容复制到 cashback-handler-v3.js 的 platform_cert 中
```

---

## 六、测试转账

### 6.1 使用uniCloud控制台测试

```javascript
// 在uniCloud控制台运行
const articleWx = uniCloud.importObject('articleWx');

// 测试单次返现
const result = await articleWx.processCashback(
  'test_record_id_123',  // 测试记录ID
  'test_user_id_456'     // 测试用户ID
);

console.log('返现结果:', result);
```

### 6.2 查看转账结果

```
方式1：微信支付商户平台
产品中心 → 商家转账到零钱 → 转账记录

方式2：API查询
调用转账明细查询接口
```

---

## 七、关键差异说明

### 7.1 批次概念

V3 API引入了"批次"概念：
- **批次（batch）**：一次转账任务，可包含多笔明细
- **明细（detail）**：批次中的单笔转账

对于单笔转账：
- 1个批次 = 1笔明细

### 7.2 场景ID

您配置的场景ID是 `1001`（分销返佣）：

```javascript
{
  transfer_scene_id: '1001' // 必须使用已配置的场景ID
}
```

**重要**：场景ID决定了转账的限额和规则。

### 7.3 签名方式

V3使用SHA256-RSA签名，安全性更高：

```
签名串格式：
HTTP方法\n
URL路径\n
时间戳\n
随机串\n
请求体\n
```

---

## 八、常见错误处理

### 错误码对照

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| PARAM_ERROR | 参数错误 | 检查请求参数格式 |
| INVALID_REQUEST | 请求非法 | 检查签名和Authorization头 |
| FREQUENCY_LIMITED | 频率超限 | 降低请求频率 |
| ACCOUNT_BALANCE_NOT_ENOUGH | 余额不足 | 充值商户账户 |
| APPID_MCHID_NOT_MATCH | appid与mchid不匹配 | 检查配置 |
| SIGN_ERROR | 签名错误 | 检查私钥和签名算法 |

### 调试技巧

```javascript
// 1. 打印签名串
console.log('签名串:', signStr);

// 2. 打印Authorization
console.log('Authorization:', authorization);

// 3. 打印请求和响应
console.log('请求:', JSON.stringify(requestData, null, 2));
console.log('响应:', JSON.stringify(response, null, 2));
```

---

## 九、生产部署检查清单

### 配置检查

- [ ] 已配置正确的 mchid
- [ ] 已配置正确的 serial_no
- [ ] 已配置正确的 apiv3_key（32位）
- [ ] 已配置完整的 private_key
- [ ] 已获取并配置 platform_cert
- [ ] transfer_scene_id 使用的是 1001（分销返佣）

### 额度检查

- [ ] 单笔金额：0.10-500元
- [ ] 单用户单日：不超过20,000元
- [ ] 商户单日：不超过100,000元

### 功能检查

- [ ] 已在沙箱/测试环境测试成功
- [ ] 已测试小额转账（<100元免密）
- [ ] 已测试大额转账（>100元需验密）
- [ ] 已实现错误重试机制

### 安全检查

- [ ] 私钥和密钥使用环境变量存储
- [ ] 平台证书定期更新
- [ ] 已实施转账频率限制
- [ ] 已添加转账日志记录

---

## 十、与您的砍价返现业务集成

### 10.1 业务流程

```
1. 用户A原价购买（100元）
   ↓
2. 用户A成为小组长
   ↓
3. 用户B帮用户A砍价（返现5元）
   ↓
4. 系统创建返现记录（cashback_status: 0）
   ↓
5. 定时任务调用V3 API转账到用户A零钱
   ↓
6. 转账成功，更新状态（cashback_status: 1）
   ↓
7. 用户A微信收到到账通知
```

### 10.2 金额限制对应

根据您的配置：
- ✅ 单笔砍价返现：0.10-500元（符合场景限制）
- ✅ 用户单日最多返现：20,000元
- ✅ 系统单日最多返现：100,000元

**建议**：
- 设置单笔砍价金额上限为100元（避免接近500元上限）
- 监控用户单日累计返现，接近20,000元时提示

---

## 十一、文档和支持

### 官方文档

- **商家转账API**：https://pay.weixin.qq.com/wiki/doc/apiv3/apis/chapter4_3_1.shtml
- **平台证书获取**：https://pay.weixin.qq.com/wiki/doc/apiv3/wechatpay/wechatpay5_1.shtml
- **签名验签**：https://pay.weixin.qq.com/wiki/doc/apiv3/wechatpay/wechatpay4_0.shtml

### 技术支持

```
客服热线：95017
在线客服：商户平台右下角
工单系统：商户平台 → 服务商管理 → 提交工单
```

---

## 十二、快速开始步骤

### 步骤1：获取配置信息（5分钟）
```
✅ mchid - 商户平台查看
✅ serial_no - 命令行查看
✅ apiv3_key - 商户平台设置
✅ private_key - 证书文件内容
```

### 步骤2：获取平台证书（5分钟）
```
✅ 运行 getCertificates.js
✅ 复制输出的证书
```

### 步骤3：配置代码（5分钟）
```
✅ 修改 cashback-handler-v3.js
✅ 更新 index.obj.js 引用
```

### 步骤4：测试转账（10分钟）
```
✅ 控制台测试小额转账
✅ 验证用户是否收到
✅ 检查转账记录
```

### 步骤5：部署上线（5分钟）
```
✅ 上传云函数
✅ 配置定时任务
✅ 监控转账日志
```

**总计：约30分钟完成配置和上线**

---

---

## 附录：您的配置已完成 ✅

### 配置完成时间
**2026年1月29日 09:56:37**

### 已配置参数
```
✅ appid:       wxf7ee79349bd957b8
✅ mchid:       1545803671
✅ serial_no:   7282272EC446E827251B9FBAC2757DE1897026D3
✅ apiv3_key:   lishuai4323811lishuai4323811lish (32位)
✅ private_key: 已配置 (1708字符)
✅ platform_cert: 已获取
```

### 配置文件位置
```
wx2/uniCloud-aliyun/cloudfunctions/articleWx/cashback-config.js
```

### 下一步
查看：**《配置完成说明.md》** 了解上传和测试步骤。

---

**文档版本**：v1.0  
**更新时间**：2026-01-29  
**适用范围**：微信支付商家转账到零钱V3 API


✅ appid     - wxf7ee79349bd957b8
✅ mchid     - 1545803671
✅ apiv3_key - lishuai4323811lishuai4323811lish


